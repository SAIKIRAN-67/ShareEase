<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Share Ease</title>
  <style>
    body {
      background: #f0f0f0;
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 0;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      color: #333;
    }
    center {
      width: 100%;
    }
    .container {
      background: white;
      padding: 2rem;
      border-radius: 8px;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
      max-width: 600px;
      width: 100%;
      text-align: center;
    }
    .container h2 {
      margin: 0 0 1rem;
      color: #4CAF50;
      font-size: 2rem;
    }
    .container h4 {
      margin: 0 0 1.5rem;
      font-size: 1.2rem;
      color: #666;
    }
    .container li {
      margin-bottom: 1rem;
      font-size: 1rem;
      list-style: none;
    }
    form {
      display: grid;
      gap: 1rem;
      grid-template-columns: 1fr 2fr;
      align-items: center;
    }
    form label {
      font-weight: bold;
    }
    form input[type="file"], 
    form input[type="password"] {
      padding: 0.5rem;
      border: 1px solid #ccc;
      border-radius: 4px;
      font-size: 1rem;
    }
    form button {
      grid-column: span 2;
      padding: 0.75rem;
      background: #4CAF50;
      color: white;
      border: none;
      border-radius: 4px;
      font-size: 1rem;
      cursor: pointer;
      transition: background 0.3s;
    }
    form button:hover {
      background: #45a049;
    }
    .file-ready {
      margin-bottom: 1rem;
      font-size: 1rem;
    }
    .file-ready a {
      color: #4CAF50;
      text-decoration: none;
      font-weight: bold;
    }
    .file-ready a:hover {
      text-decoration: underline;
    }
    @media (max-width: 500px) {
      .container {
        padding: 1rem;
      }
      form {
        grid-template-columns: 1fr;
      }
      form label, form input, form button {
        grid-column: span 1;
      }
    }
  </style>
</head>
<body>
  <center>
    <div class="container">
      <div class="typing">
        <h2 class="text-uppercase">Welcome to ShareEase</h2>
      </div>
      <div class='content'>
        <h4>ShareEase is a platform to share your files with security password</h4>
        <li>Simply upload a file, share the link, and after it is downloaded, the file is completely deleted.</li>
      </div>
      <form action="/upload" method="post" enctype="multipart/form-data">
        <label for="file">File:</label>
        <input type="file" id="file" name="file" required />
        <label for="password">Password:</label>
        <input type="password" id="password" name="password" />
        <button type="submit">Share</button>
      </form>
      <% if (locals.fileLink != null) { %>
        <div class="file-ready">
          Your file is ready at
          <a href="<%= locals.fileLink %>"><%= locals.fileLink %></a>
        </div>
      <% } %>
    </div>
  </center>
</body>
</html>
